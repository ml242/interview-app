const Product = () => {
    //  const productId = await getProduct(typeof params.id === 'string' ? params.id : params.id[0]);

    // const product = await getProducts().then((products) => {
    //   return products.devices.find((item:any) => item.id === productId.id);
    // })

    // if (!product) return <h1>We must be sold</h1>;

    return (
      <h1>Hello the id is 
        {/* {product.id} */}
      </h1>
    );
  }

  export default Product;